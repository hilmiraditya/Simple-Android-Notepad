<?php

$host = "localhost";
$user = "root";
$password = "";
$dbname = "retrofit_crud";

$conn = mysqli_connect($host, $user, $password, $dbname);

if (!$conn) {
	die("error in connection");
}else{

}
?>